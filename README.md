# 🚀 LifeOS - نظام إدارة المهام والكتابة المتكامل

نظام شخصي متكامل لإدارة المهام، الأهداف، والكتابة الإبداعية، مصمم ليعمل بكفاءة عالية على السحاب مع واجهة مستخدم عصرية وسلسة.

## 🌟 المميزات الحالية
* **نظام إتمام المهام الذكي**: دوائر تفاعلية تتيح لك تعليم المهام كمكتملة مع مزامنة فورية لقاعدة البيانات.
* **الأرشفة المتقدمة**: صفحة مستقلة بنظام SPA (Single Page Application) تتيح أرشفة المهام والملفات وتجميعها في "بلوكات" منظمة حسب المشروع أو التاريخ.
* **نظام Blueprints**: تنظيم احترافي للكود البرمجي (Auth, Tasks, Writing, Settings) لسهولة التطوير.
* **التخصيص الشخصي**: إعدادات متقدمة لتغيير السمات (Themes) والألوان مع خاصية الحفظ الدائم في MongoDB و LocalStorage.

## 🛠 التكنولوجيا المستخدمة
* **Backend**: Python (Flask).
* **Database**: MongoDB Atlas (Cloud).
* **Authentication**: Flask-JWT-Extended.
* **Deployment**: Vercel.
* **Frontend**: HTML5, CSS3, JavaScript (Vanilla).

## 📂 هيكل المجلدات

```text
/
├── server.py             # الملف الرئيسي لتشغيل التطبيق
├── vercel.json           # إعدادات التوجيه والرفع لـ Vercel
├── requirements.txt      # المكتبات البرمجية المطلوبة
├── routes/               # ملفات الـ Blueprints (api, auth, tasks...)
└── web/                  # ملفات الواجهة الأمامية
    ├── static/           # ملفات الـ CSS والـ JS والصور
    └── templates/        # قوالب HTML (index, archive...)
🚀 التشغيل المحلي
قم بتحميل المستودع (Clone):

Bash
git clone [https://github.com/your-username/lifeOS1.git](https://github.com/your-username/lifeOS1.git)
قم بتثبيت المكتبات:

Bash
pip install -r requirements.txt
قم بتشغيل السيرفر:

Bash
python server.py
⚙️ متغيرات البيئة (Environment Variables)
يجب إعداد المتغيرات التالية في Vercel لضمان عمل قاعدة البيانات:

MONGO_URI: رابط الاتصال بـ MongoDB Atlas.

JWT_SECRET_KEY: مفتاح تشفير البيانات الخاص بك.

تم تطويره بكل ❤️ بواسطة [اسمك هنا]


---

### نصيحة إضافية:
عند رفع هذا الملف إلى GitHub، سيظهر بشكل منسق جداً في واجهة المستودع، مما يعطي انطباعاً احترافياً عن مشروعك.

**هل تريد مني إضافة قسم خاص بـ "خطة التطوير المستقبلية" (Roadmap) تذكر فيها الميزات التي تنوي إضافتها لاحقاً؟**
