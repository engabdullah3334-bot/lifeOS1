/**
 * app.js — DEPRECATED / EMPTY
 * ─────────────────────────────────────────────────────────────
 * This file was the original monolithic app entry point.
 * It has been fully replaced by:
 *   - web/static/js/main.js          ← SPA router & init
 *   - web/static/js/modules/*.js     ← Feature modules
 *   - web/static/js/modules/tasks/   ← Task system
 *
 * Kept as an empty file to avoid 404 if any external link
 * references it. Safe to delete in a future cleanup pass.
 */
